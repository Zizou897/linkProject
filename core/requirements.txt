# ── Framework ──────────────────────────────────────────────
Django==5.2.15

# ── Serveur & fichiers statiques ───────────────────────────
gunicorn==23.0.0
whitenoise[brotli]==6.12.0

# ── Base de données (MySQL en prod) ────────────────────────
mysqlclient==2.2.8

# ── API / divers ───────────────────────────────────────────
django-cors-headers==4.9.0
python-decouple==3.8
pillow==12.2.0

# ── Tâches asynchrones ─────────────────────────────────────
# after_response : envoi des notifications après la réponse HTTP (thread)
django-after-response==0.2.2
# Celery (broker SQLAlchemy sur la DB) : nettoyage planifié des formulaires invités
celery==5.6.3
sqlalchemy==2.0.50
django-celery-results==2.6.0

# ── Exports & génération ───────────────────────────────────
openpyxl==3.1.5
qrcode[pil]==8.2
